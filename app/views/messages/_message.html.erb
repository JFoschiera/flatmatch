<div>
  <small>
    <strong><%= message.user.name %></strong>
  </small>
    <p class="small p-2 ms-3 mb-2 rounded-pill" style="background-color: #f0520a;"><%= message.content %></p>
  <small>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
</div>
